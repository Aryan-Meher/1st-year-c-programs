//program to explore the size of data types, such as the size of int, float, double, and char, based on the user's choice value
#include <stdio.h>
int main()
            {    int var;
                 printf("size of integer, float, double,character are: %d, %d, %d, %d", sizeof(int),sizeof(float),sizeof(double),sizeof(char));
    printf("\nsize of variable var is: %d ", sizeof(var));
    return 0;
}

